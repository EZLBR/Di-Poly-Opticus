# ============================================================
#   OPTICUS BACKEND — Variáveis de Ambiente
#   ⚠️  NUNCA commitar este arquivo! Adicione .env ao .gitignore
#
#   Railway MySQL — mapeamento de variáveis:
#     MYSQLHOST      → DB_HOST
#     MYSQLPORT      → DB_PORT
#     MYSQLUSER      → DB_USER
#     MYSQLPASSWORD  → DB_PASSWORD
#     MYSQLDATABASE  → DB_NAME
# ============================================================

# ── Banco de Dados MySQL ────────────────────────────────────
DB_HOST=zephyr.proxy.rlwy.net
DB_PORT=12571
DB_USER=root
DB_PASSWORD=pFhspCUGznCeiKQWUIQtbaeNRQnFQKCI
DB_NAME=railway

# ── Autenticação JWT ────────────────────────────────────────
JWT_SECRET=JWT_SECRET=opticus_jwt_super_secreto_2026

# ── Pagamentos AbacatePay (deixe em branco para usar simulador) ──
ABACATE_TOKEN=

# ── Servidor ────────────────────────────────────────────────
PORT=5000
